## AI 简历内容生成器 — 产品需求文档 (PRD)

> 项目性质：个人练手项目 | 版本：v0.2 | 日期：2026-07-13

---

### 一、产品定位

**一句话定义**：用户填写基本信息并粘贴目标岗位的 JD，AI 生成高度匹配岗位的简历内容（非排版），用户复制粘贴到自己的简历模板中使用。

核心价值不是"帮你排版简历"，而是**降低从"我有什么经历"到"一段能打动面试官的文字"之间的摩擦力**。我们解决的是"写什么"和"怎么写"的问题，排版交给用户自己选的模板。

**v0.2 版本重大调整**：砍掉 PDF/DOCX 导出功能，产品聚焦于"简历内容生成"。原因：排版导出是我们的弱项（中文断行、字体兼容、分页控制等技术坑多），而市面上已有大量免费简历模板工具（WPS、超级简历等），用户不缺模板，缺的是能针对 JD 写出高质量内容的能力——这正是 AI 最擅长的。

---

### 二、目标用户与痛点

**主要用户画像**：

1. **应届生 / 实习生**——有零散的校园经历、项目经验，但不知道怎么组织成简历语言，面对空白页面无从下手。
2. **转行 / 跨岗求职者**——有经历但不知道如何针对目标岗位重新措辞和突出重点。
3. **批量求职者**——同时投多个岗位，需要为每个岗位定制简历内容，重复劳动大。

**核心痛点**：

| 痛点 | 现有解决方式 | 问题 |
|------|-------------|------|
| 不知道怎么写 | 找模板手填、看教程 | 耗时 2-4 小时，质量不稳定 |
| 写出来不像简历 | 花钱找代写（50-200 元） | 贵，且不了解你，沟通成本高 |
| 一份简历投所有岗位 | 同一份海投 | 匹配度低，回复率差 |
| 经历跟 JD 对不上 | 自己对照 JD 逐条改 | 不知道怎么重构叙事角度 |

---

### 三、产品形态

**网页端（Web App）**

理由：开发周期最短，一个人就能搞定前后端；无需审核上架，部署即上线；浏览器内完成全部流程，用户零安装成本；分享传播方便，一个链接就能用。

---

### 四、MVP 功能范围（v0.2）

#### 做什么（P0 - 必须有）

1. **引导式信息填写**——分步表单：基本信息 → 目标岗位/JD → 教育经历 → 经历（工作经历 + 项目经历可混合添加）→ 技能证书。每步有占位提示，降低填写门槛。
2. **JD 粘贴与深度匹配**——用户粘贴招聘信息，AI 以 JD 为分析框架，重构每段经历的叙事角度，确保每个 bullet 都在回应 JD 的某个具体要求。
3. **AI 智能内容生成**——基于用户原始信息 + JD，AI 输出以下板块的完整内容：
   - **求职意向**：一行精炼的岗位意向（目标岗位 + 城市 + 到岗时间）
   - **职业概述 + 核心优势**：1-2 句概述（第一句直击 JD 核心需求）+ 3 个核心优势要点
   - **工作经历 / 项目经历**：每段经历 STAR 格式改写，每条 bullet 附带 `match_reason`（标注这条在回应 JD 的哪个要求）
   - **技能清单**：按类别分组（编程语言 / 框架工具 / 数据库 / 语言能力）
   - **证书**：事实性凭证原样列出
   - **自我评价**：3-4 句话，根据 JD 看重的软素质定制（性格、态度、职业追求）
   - **面试话术 / 准备建议**：针对每段经历生成"面试官可能问什么 + 怎么回答"
4. **内容筛选与复制**——每个板块独立复制按钮（格式化纯文本，直接粘贴到 WPS）；顶部一键复制全文；每条 bullet 可勾选（取消勾选的不纳入复制内容）。
5. **JD 匹配分析报告**——展示 JD 核心要求、各经历匹配度、用户当前短板（gaps），帮用户了解自己在 JD 面前的竞争力。

#### 先不做

| 功能 | 不做的原因 |
|------|-----------|
| PDF / DOCX 导出 | 排版是弱项，用户用 WPS 模板效果更好；聚焦内容生成 |
| 简历视觉模板 | 不做导出就不需要模板渲染，预览页用干净的卡片式布局即可 |
| 用户账号体系 | 练手项目不需要，用本地存储 |
| 简历投递 / ATS 评分 | 复杂度高，需要大量招聘数据支撑 |
| 多语言简历 | 先做好中文 |
| 简历数据分析 | 没有账号体系就无法追踪 |
| 从已有简历解析导入 | PDF 解析准确率低，坑多 |

---

### 五、页面清单

整个产品 **3 个有效页面**，极度克制：

| 页面 | 功能 | 备注 |
|------|------|------|
| 首页 (Landing) | 产品介绍 + "开始制作"按钮 | 简洁，30 秒看懂产品是干嘛的 |
| 信息填写页 (FormPage) | 5 步表单收集用户信息 | 基本信息 → 目标岗位/JD → 教育 → 经历 → 技能证书 |
| 内容结果页 (ResultPage) | AI 生成结果展示 + 筛选 + 复制 | 核心页面，停留时间最长 |

---

### 六、核心用户流程

```
用户进入首页
    │
    ▼
点击"开始生成简历内容"
    │
    ▼
分步填写个人信息（约 5-8 分钟）
  ① 基本信息：姓名、联系方式
  ② 目标岗位：岗位名称 + 粘贴 JD（推荐）
  ③ 教育经历：学校、专业、GPA、相关课程
  ④ 工作/项目经历：公司/项目名、时间、做了什么（随意写）
  ⑤ 技能与证书：技术栈、语言能力、证书
    │
    ▼
点击"AI 生成简历内容"
    │
    ▼
AI 处理（15-30 秒）
  - 深度分析 JD，提取硬性要求 + 软性偏好 + 岗位画像
  - 对每段经历选择最贴合 JD 的叙事角度
  - 重写为 STAR 格式，每条 bullet 标注匹配的 JD 要求
  - 生成求职意向、职业概述、核心优势、自我评价
  - 生成面试话术和准备建议
    │
    ▼
内容结果页
  - 各板块以卡片形式展示，每张卡片右上角有"复制"按钮
  - 每条 bullet 前有 checkbox，可勾选/取消
  - 顶部有"一键复制全文"按钮
  - JD 匹配分析报告在顶部或侧边展示
    │
    ▼
用户复制内容 → 粘贴到自己的简历模板（WPS/超级简历等）
```

**关键设计原则**：从进入页面到拿到可复制的内容，控制在 **10 分钟以内**。

---

### 七、AI 内容生成详细设计

#### 7.1 输出板块结构

| 板块 | 数据结构 | 说明 |
|------|---------|------|
| 求职意向 | `job_objective: str` | 一行话，如"求职意向：Java后端开发工程师 · 广州 · 2周内到岗" |
| 职业概述 | `career_summary: str` | 1-2 句话概述专业背景，第一句直击 JD 核心需求 |
| 核心优势 | `core_strengths: list[str]` | 3 个要点，每个一句话，强调跟 JD 最匹配的亮点 |
| 工作经历 | `experience: list[Exp]` | 每段含 company/role/dates + bullets（每条 bullet 附 match_reason） |
| 项目经历 | `project_experience: list[Exp]` | 同上，name 替代 company/role |
| 技能清单 | `skills: SkillGroup[]` | 按类别分组，如 { category: "编程语言", items: ["Java", "Python"] } |
| 证书 | `certificates: list[str]` | 事实性凭证原样列出 |
| 自我评价 | `self_evaluation: str` | 3-4 句话，根据 JD 看重的软素质定制 |
| 面试话术 | `interview_tips: list[Tip]` | 每段经历对应 2-3 个面试问题 + 回答思路 |
| JD 分析 | `jd_analysis: JDAnalysis` | key_requirements / matched_projects / gaps |

#### 7.2 Bullet 的 match_reason 设计

每条经历 bullet 不再是单纯的字符串，而是带元数据的结构：

```json
{
  "text": "基于 Spring Boot 重构订单管理系统的核心接口，优化查询/创建/更新流程，接口响应时间降低约 40%",
  "match_reason": "对应 JD 中'Spring Boot 框架开发经验'和'接口设计与优化能力'的要求",
  "match_score": "high"
}
```

**前端展示**：bullet 下方用浅色小字显示 match_reason，点击可折叠。让用户清楚知道"这条为什么这么写"，有依据地决定保留或删除。

#### 7.3 技能分类输出

AI 输出技能时按类别分组，而非扁平列表：

```json
{
  "skills": [
    { "category": "编程语言", "items": ["Java（熟练）", "Python（熟悉）", "JavaScript（了解）"] },
    { "category": "框架/工具", "items": ["Spring Boot", "React", "Git"] },
    { "category": "数据库", "items": ["MySQL（含查询优化经验）", "Redis"] },
    { "category": "语言能力", "items": ["英语 CET-4"] }
  ]
}
```

#### 7.4 Prompt 策略（v2）

已实现的核心改进：

1. **JD 前置**：JD 内容放在 prompt 最前面（用户信息之前），作为 AI 分析的基底框架
2. **深度分析优先**：要求 AI 先做 JD 深度分析（硬性要求 / 软性偏好 / 岗位画像 / 高频关键词），再逐段匹配
3. **重框架化叙事**：不是硬塞关键词，而是从三个维度匹配（技术栈重合、能力维度重合、业务场景相似），选择最贴合 JD 的叙事角度
4. **放宽"不编造"约束**：允许对真实经历进行重新组织、重点突出、角度调整、细节补充，只禁止凭空编造项目经历
5. **自我验证步骤**：要求 AI 生成后默检每个 bullet 是否真正回应了 JD 要求
6. **调参**：JD 模式 temperature 0.5（鼓励创造性重构），max_tokens 4000

---

### 八、复制粘贴体验设计

这是 v0.2 最核心的 UX 环节——用户拿到的不是文件，而是文本，必须让复制粘贴的体验极度顺滑。

#### 8.1 板块级复制

每个内容板块（求职意向、概述、经历、技能、自我评价等）右上角有一个"复制"图标按钮。点击后：
- 将该板块格式化为**纯文本**（带标题、分隔、bullet 符号），写入系统剪贴板
- 按钮变为"已复制 ✓"，1.5 秒后恢复
- 格式示例（工作经历复制到剪贴板）：
  ```
  XX科技 | Java后端实习生 | 2025.07 - 2025.09
  · 基于 Spring Boot 重构订单管理系统的核心接口...
  · 引入 Redis 缓存机制，缓存高频访问的订单数据...
  · 修复订单状态同步、库存扣减等关键业务逻辑 Bug...
  ```

#### 8.2 一键复制全文

顶部大按钮"复制全部简历内容"。点击后：
- 将所有板块按标准简历顺序拼接为纯文本
- 只包含已勾选的 bullets
- 写入剪贴板，提示"已复制，可粘贴到 WPS 等工具中"

#### 8.3 Bullet 勾选筛选

每条经历 bullet 前有 checkbox：
- 默认全部勾选
- 用户取消勾选的 bullet 变灰，不纳入复制内容
- 配合 match_reason 使用：用户看到"这条跟 JD 没啥关系"→ 取消勾选 → 复制时自动跳过

#### 8.4 在线微调

每个文本字段仍支持直接编辑（点击即可修改），修改后复制按钮获取的是编辑后的内容。这是对 AI 输出的兜底——AI 写得不对的地方用户可以手动改。

---

### 九、技术方案

#### 技术选型

| 层级 | 选型 | 理由 |
|------|------|------|
| 前端框架 | React 19 + Vite | 生态成熟，组件化开发，Vite 构建快 |
| UI 样式 | Tailwind CSS v3.4 | 轻量原子化 CSS，无需组件库 |
| 状态管理 | Zustand | 轻量，比 Redux 简洁 |
| 后端框架 | Python FastAPI | AI 集成最方便（Python 生态），性能好 |
| AI 模型 | DeepSeek API | 中文能力强，性价比极高（约 ¥1/百万 token） |
| 数据库 | 暂不用 | 数据存前端 localStorage |
| 部署 | Vercel (前端) + Railway/Render (后端) | 免费额度足够练手用 |

#### 系统架构

```
┌─────────────┐     HTTP/JSON     ┌──────────────┐    Prompt     ┌───────────┐
│  React 前端  │ ───────────────→ │ FastAPI 后端  │ ───────────→ │  AI 模型   │
│  (Vite 部署) │ ←─────────────── │  (Render 部署)│ ←─────────── │ (DeepSeek) │
└─────────────┘                   └──────────────┘               └───────────┘
       │
       │  localStorage
       │  (用户数据暂存)
       │
       │  剪贴板 API
       ▼
  用户复制到 WPS 等工具
```

前端负责数据收集、内容展示、剪贴板操作。后端只做一件事：接收结构化数据 → 组装 prompt → 调用 AI → 返回结构化简历内容 JSON。

#### AI 方案（成本控制）

生成一份简历约消耗 3000-5000 tokens（含 JD 分析），按 DeepSeek 价格算，单次生成成本约 ¥0.003-0.008，几乎可以忽略。

---

### 十、成本估算

| 项目 | 月费用 | 说明 |
|------|--------|------|
| 域名 | ¥0 | 用 Vercel 免费子域名 |
| 前端部署 | ¥0 | Vercel 免费额度 |
| 后端部署 | ¥0 | Render 免费版 750 小时/月 |
| AI API | ¥0-10 | DeepSeek 按量付费，日均 100 次 ≈ ¥3-5/月 |
| **合计** | **¥0-10/月** | 零成本可控 |

---

### 十一、主要风险与应对

**1. AI 生成质量不稳定**

风险：AI 可能生成空洞的套话、捏造经历、或对某些岗位的理解偏差大。

应对：精细化 prompt（v2 已实现 JD 前置 + 重框架化叙事 + 自验步骤）；match_reason 让用户能判断每条 bullet 的质量；在线编辑 + bullet 勾选兜底。

**2. 用户不知道拿复制的内容怎么用**

风险：用户习惯了"导出文件"的模式，对"复制粘贴"感到困惑。

应对：首页明确说明"生成内容 → 复制到你的简历模板"；结果页顶部引导文案；提供"推荐模板工具"链接（WPS 免费简历模板、超级简历等）。

**3. 用户填写意愿低**

风险：表单太长用户放弃填写。

应对：分步引导、每步只展示当前部分；支持从其他简历复制粘贴经历描述；项目经历可以用"导入项目文件"自动分析。

**4. JD 匹配度不够高**

风险：即使用了 v2 prompt，某些岗位（如跨行业）的经历确实难以匹配。

应对：JD 分析报告诚实展示 gaps；面试话术帮用户准备"如何解释跨行业经历"；允许用户手动调整 bullet 角度。

---

### 十二、MVP 成功标准

1. 用户能在 10 分钟内走完流程并获得可复制的简历内容
2. AI 生成的经历描述与 JD 高度相关（match_reason 准确反映匹配逻辑）
3. 复制出来的纯文本格式粘贴到 WPS 后可直接使用，无需大量格式调整
4. 全流程无报错，不需要开发者介入

---

### 十三、已完成的功能（v0.1 → v0.2 演进记录）

| 功能 | 状态 | 说明 |
|------|------|------|
| 引导式 5 步表单 | ✅ 已完成 | 基本信息→目标岗位/JD→教育→经历→技能证书 |
| 工作经历/项目经历分离 | ✅ 已完成 | 双卡片自由混合添加 |
| JD 粘贴（Tab 切换） | ✅ 已完成 | 手动输入岗位名 / 粘贴 JD 两种模式 |
| AI STAR 格式改写 | ✅ 已完成 | prompt v2 已升级为 JD 驱动式 |
| 技能/证书 AI 分类润色 | ✅ 已完成 | skills: list[str] + certificates: list[str] |
| 项目深度分析（文件上传） | ✅ 已完成 | zip/docx/pdf 解析 + 面试准备清单 |
| 预览页在线编辑 | ✅ 已完成 | 所有文本可编辑，改动实时同步 |
| 双模板切换 | ✅ 已完成（待清理） | Minimal + Business 模板，砍导出后改为纯内容展示 |
| PDF 导出 (html2canvas+jsPDF) | ❌ 待清理 | v0.2 砍掉 |
| DOCX 导出 (python-docx) | ❌ 待清理 | v0.2 砍掉 |
| 可视化编辑器 (EditorPage) | ⏸️ 搁置 | 独立 editorStore，与主流程无关，暂不处理 |

---

### 十四、待开发功能（v0.2 增量）

| 功能 | 优先级 | 说明 |
|------|--------|------|
| **新增板块：求职意向** | P0 | AI 生成一行精炼意向 |
| **新增板块：自我评价** | P0 | 根据 JD 软素质定制 3-4 句话 |
| **新增板块：面试话术** | P0 | 每段经历 2-3 个面试问题 + 回答思路 |
| **总结拆分：职业概述 + 核心优势** | P0 | 替代当前扁平的 summary |
| **Bullet 附 match_reason** | P0 | 每条 bullet 标注匹配的 JD 要求 |
| **技能分类输出** | P0 | 按类别分组（语言/框架/数据库/其他） |
| **板块级复制按钮** | P0 | 每个板块独立复制为格式化纯文本 |
| **一键复制全文** | P0 | 所有板块按简历顺序拼为纯文本 |
| **Bullet 勾选筛选** | P0 | checkbox 控制哪些 bullet 纳入复制 |
| **清理导出相关代码** | P0 | 移除 PDF/DOCX 按钮、后端 export 路由 |
| **预览页改为结果页** | P1 | 去掉模板渲染，改为卡片式内容展示 |
| **JD 匹配分析报告增强** | P1 | 匹配度评分 + 短板改进建议 |

---

### P1 功能一：项目深度分析 + 面试准备清单

> 状态：✅ 已开发完成 | 开发于 2026-06-25

#### 功能概述

用户可上传项目文件（.zip / .docx / .pdf / .txt / .md），AI 深度分析后自动生成：
- STAR 格式的简历经历描述
- 自动识别技术栈和用户角色定位
- 面试准备清单（核心知识点 + 可能被问到的问题 + 回答提示）

#### 技术方案

后端分层分析策略（解决项目代码超出上下文窗口的问题）：
1. **全局理解层**：目录结构 + README + 依赖配置（pom.xml / package.json）
2. **核心模块层**：筛选 3-5 个核心文件（Controller/Service/核心逻辑），跳过工具类/配置/测试
3. **生成输出层**：合并两层分析结果，生成 experience + interview_prep

#### 已实现文件

- `backend/services/file_parser.py`：文件解析层
- `backend/services/analysis_service.py`：分层分析逻辑
- `backend/routers/analysis.py`：`POST /api/analyze` 接口

---

### P1 功能二：JD 岗位匹配

> 状态：✅ 已开发完成，prompt 已升级至 v2 | 开发于 2026-06-25，升级于 2026-07-13

#### 功能概述

用户粘贴招聘信息（从 Boss 直聘、拉勾等平台复制），AI 以 JD 为分析框架，精准调整简历的叙事角度和关键词。

#### Prompt v2 核心改进（2026-07-13）

- JD 前置为分析框架（而非事后补丁）
- "重框架化叙事"替代"关键词硬塞"
- 三维度匹配：技术栈重合 + 能力维度重合 + 业务场景相似
- 叙事角度选择示例（同一项目按 JD 需求突出不同方面）
- 自我验证步骤（生成后默检每个 bullet）
- 放宽"不编造"约束（允许重组/突出/调整角度/补充细节）

---

### P1 功能三：经历类型分离

> 状态：✅ 已开发完成 | 开发于 2026-06-25

#### 功能概述

工作经历和项目经历分开添加，可自由混合。项目经历卡片更精简（只有名称+时间+描述），降低应届生填写门槛。

---

### 附：已废弃的功能（v0.2 清理清单）

以下功能在 v0.1 中已实现，v0.2 中需要清理代码：

| 功能 | 涉及文件 | 清理方式 |
|------|---------|---------|
| PDF 导出 | `PreviewPage.jsx` 中 html2canvas + jsPDF 逻辑 | 移除按钮和处理函数，移除 npm 依赖 |
| DOCX 导出 | `backend/routers/export.py` + `backend/services/docx_service.py` | 删除文件，移除 main.py 路由注册 |
| 前端导出 API | `frontend/src/services/api.js` 中 `exportDocx()` | 移除函数 |
| 模板切换器 | `PreviewPage.jsx` 中模板切换 UI | 移除，预览改为统一卡片布局 |
| A4 页数估算 | `PreviewPage.jsx` 中页数显示 | 移除 |
| Token 消耗展示 | `PreviewPage.jsx` 中 token 显示 | 保留（对用户有参考价值） |
